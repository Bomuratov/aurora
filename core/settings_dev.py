from .settings import *

DEBUG = True

DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": "aurora",
        "USER": "aurora",
        "PASSWORD": "N4:>g0z}r2Xx",
        "HOST": "localhost",
        "PORT": "5432",
    }
}
